# FastAPI backend
fastapi==0.115.9
uvicorn==0.23.2
python-multipart==0.0.6

# PDF parsing
PyPDF2==3.0.1

# Embeddings & vector DB (prebuilt wheels only)
chromadb==1.1.0

# Sentence Transformers & Torch (CPU-only wheels to avoid compilation)
torch==2.8.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==2.2.2

# LLM / summarization
transformers==4.42.2

# Environment variables
python-dotenv==1.1.1
